#ifndef LIGHTGEBRA_MATRIX_HPP
#define LIGHTGEBRA_MATRIX_HPP

#include <vector>

template <typename Number>
class DynMatrix
{
    public:
	Matrix(int dim);
	Matrix(int lines, int columns);

	// default copy/move/destructor/ctor (cf rule or 0/5)

	Matrix operator*
};

#endif
